#ifndef FILTEREDLIST_H
#define FILTEREDLIST_H

class FilteredList;

//We store filtered lists as a tree.
//The leaves of the tree store the actual points, together with associated data.
//The type of associated data at the leaves depends on the actual algorithm (i.e. on k)
//


class FilteredList{



};

#endif
